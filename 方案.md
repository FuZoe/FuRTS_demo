# FuRTS (像素RTS游戏) 开发方案

## 项目背景

本项目旨在开发一款致敬《星际争霸》或《红色警戒》的像素风格即时战略(RTS)游戏。经过对比分析，选择从《红色警戒》模式入手，因为其开发工作量相对较小，更适合独立开发者。

### 核心设计原则
- **简化开发**：尽可能简单，网页直接运行，无需环境配置
- **白盒测试**：美术部分完全不做，用文字代替各种元素
- **快速验证**：先验证核心玩法是否有趣，再考虑视觉表现

## 技术选型分析

### 是否使用游戏引擎框架？
**结论：不需要传统的重度引擎**

不推荐使用：
- Unity、Unreal、Godot 等大型引擎
- 原因：需要下载几GB软件，学习复杂UI界面，违背"零环境配置"初衷

推荐使用：
- Vanilla JavaScript（原生JS）+ HTML5 Canvas
- 只需一个 index.html 文件，浏览器直接运行

## 三种技术方案对比

### 方案A：原生 HTML5 Canvas + JS（强烈推荐）

**优势：**
- 绝对轻量级，零依赖
- 完全掌控底层逻辑
- 有助于理解RTS游戏系统（单位遍历、状态更新）
- 符合"尽可能简单、无需配环境"的需求

**实现思路：**
- 使用 `ctx.fillText("工", x, y)` 在屏幕上画字符
- 使用 `requestAnimationFrame` 实现60FPS游戏循环
- 直接操作DOM和Canvas API

### 方案B：使用 Rot.js

**优势：**
- 专为浏览器开发Roguelike（纯字符画面）游戏设计
- 天生为渲染ASCII字符网格而生
- 内置字体大小、网格对齐、视野计算功能

**适用场景：**
- 直接把屏幕当成二维数组
- 定义 B=基地, *=矿石, w=农民 等字符映射

### 方案C：使用 Phaser.js / Kaboom.js

**优势：**
- 专门的2D网页游戏框架
- 内置碰撞检测、按键输入、简单物理引擎
- 便于未来从文字升级到像素图片

**实现方式：**
- 先用 Text Object 渲染文字
- 机制完成后直接替换为 Sprite

## 推荐方案

**选择方案A：原生 HTML5 Canvas + JS**

**理由：**
1. 最符合"零环境配置"需求
2. 学习成本低，只需基础JavaScript知识
3. 完全掌控底层，便于理解RTS核心机制
4. 性能最优，无额外依赖
5. 便于后续根据需要迁移到其他框架

## 文字版RTS概念模型

### 画面示例
```
[资源区]           [交战区]                 [你的基地]
 * * * * * * w(移动中)            e(敌军)        B(主基地)
 * * * ^
  [矿]                             t(坦克)     |(生产线)
```

### 核心架构

#### 数据状态（State）
用数组记录单位数据：
```javascript
{
  ID: 1,
  类型: "农民",
  X: 10,
  Y: 20,
  血量: 50,
  状态: "采矿中"
}
```

#### 游戏循环（Game Loop）
每秒运行60次，每次执行：

1. **Update（更新逻辑）**
   - 遍历所有单位
   - 根据速度改变X/Y坐标
   - 扣除被攻击的血量

2. **Render（渲染画面）**
   - 清空浏览器屏幕
   - 将单位数据渲染为字符
   - 在指定位置绘制文字

## 开发路线图

### 第一阶段：基础框架
- [ ] 创建HTML5 Canvas基础页面
- [ ] 实现游戏循环（Game Loop）
- [ ] 建立单位数据结构

### 第二阶段：核心功能
- [ ] 绘制网格地图
- [ ] 生成主基地字符
- [ ] 实现鼠标点击移动
- [ ] 添加农民单位

### 第三阶段：RTS机制
- [ ] 资源采集系统
- [ ] 建筑建造系统
- [ ] 单位生产队列
- [ ] 基础战斗系统

### 第四阶段：优化与扩展
- [ ] 寻路算法
- [ ] 单位状态管理
- [ ] 简单AI
- [ ] 性能优化

## 技术难点预判

1. **寻路算法**：大规模单位的群体寻路
2. **单位状态管理**：多单位并发状态同步
3. **性能优化**：大量单位时的渲染效率
4. **碰撞检测**：单位间的物理交互

## 参考资料

- 《星际争霸》vs《红色警戒》设计哲学对比
- HTML5 Canvas API 文档
- RTS游戏核心机制研究
- 白盒测试/原型开发方法论

## 备注

本方案基于与Gemini的讨论记录整理，核心思想是"先验证好不好玩，再考虑好不好看"，通过纯文字原型快速迭代RTS游戏核心玩法。